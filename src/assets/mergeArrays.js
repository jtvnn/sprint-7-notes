[1, 3, 5, 7, 9], [2, 4, 6, 8];

function mergeArrays(array1, array2) {
  // 1 pointer (i) at array1
  // 2nd pointer (j) at array2

  // you check if array1[i] < array2[j]

  return [1, 2, 3];
}

O(m + n);

// concat + sort minimum time complexity of O((m+n)^2)
